name: 'jitgen'
description: 'Just-in-time LLM regression test generation'
inputs:
  parent-ref:
    description: 'Parent git ref (base branch)'
    required: true
  child-ref:
    description: 'Child git ref (feature branch head)'
    required: true
  pr-number:
    description: 'Pull request number to comment on'
    required: false
  llm-api-key:
    description: 'API key for the LLM provider'
    required: true
  llm-endpoint:
    description: 'LLM API endpoint URL'
    required: true
  llm-model:
    description: 'LLM model name'
    required: true
  github-token:
    description: 'GitHub token for pushing branches and creating PRs'
    required: false
    default: ${{ github.token }}
runs:
  using: 'docker'
  image: 'Dockerfile'
  env:
    LLM_API_KEY: ${{ inputs.llm-api-key }}
    LLM_ENDPOINT: ${{ inputs.llm-endpoint }}
    LLM_MODEL: ${{ inputs.llm-model }}
    GITHUB_TOKEN: ${{ inputs.github-token }}
    GITHUB_PR_NUMBER: ${{ inputs.pr-number }}
  args:
    - --repo
    - /github/workspace
    - --parent
    - ${{ inputs.parent-ref }}
    - --child
    - ${{ inputs.child-ref }}
    - --debug
